{
  "name": "@derivo/dependency-health",
  "version": "0.1.0",
  "description": "Validates lockfiles, package versions, and peer dependencies",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext ts,tsx --max-warnings 0",
    "dev": "tsc -w"
  },
  "type": "module",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "@derivo/config-eslint": "workspace:*",
    "@derivo/config-typescript": "workspace:*"
  }
}
