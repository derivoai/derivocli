{
  "name": "@derivo/container-health",
  "version": "0.1.0",
  "description": "Inspects Docker, Compose, networks, and container status",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext ts,tsx --max-warnings 0",
    "dev": "tsc -w"
  },
  "type": "module",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "@derivo/config-eslint": "workspace:*",
    "@derivo/config-typescript": "workspace:*"
  }
}
